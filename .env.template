# =============================================================================
# CLASH ROYALE DECK BUILDER - CENTRALIZED ENVIRONMENT CONFIGURATION
# =============================================================================
# This is the master template for all environment variables.
# Copy this file to create environment-specific configurations:
# - .env (for active development - auto-detected)
# - .env.development (for local development)
# - .env.docker (for containerized development)  
# - .env.production (for production deployment)
#
# SECURITY WARNING: Never commit actual .env files to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# ENVIRONMENT IDENTIFICATION
# -----------------------------------------------------------------------------
# Environment type: development, docker, production
ENVIRONMENT=development

# Application metadata
APP_NAME=Clash Royale Deck Builder
APP_VERSION=1.0.0

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------
# MySQL database connection settings
DB_HOST=localhost
DB_PORT=3306
DB_NAME=clash_deck_builder
DB_USER=clash_user
DB_PASSWORD=your_secure_password_here
DB_ROOT_PASSWORD=your_secure_root_password_here

# Database performance settings
DB_POOL_SIZE=10
DB_CHARSET=utf8mb4
DB_COLLATION=utf8mb4_unicode_ci

# -----------------------------------------------------------------------------
# BACKEND API CONFIGURATION
# -----------------------------------------------------------------------------
# Server settings
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# Application behavior
DEBUG=false
LOG_LEVEL=info

# CORS configuration (comma-separated list)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# -----------------------------------------------------------------------------
# FRONTEND CONFIGURATION
# -----------------------------------------------------------------------------
# Frontend development server
FRONTEND_PORT=3000
GENERATE_SOURCEMAP=true
BROWSER=none

# API connection
REACT_APP_API_BASE_URL=http://localhost:8000

# -----------------------------------------------------------------------------
# EXTERNAL API CONFIGURATION
# -----------------------------------------------------------------------------
# Clash Royale API
# Get your key from: https://developer.clashroyale.com/
CLASH_ROYALE_API_KEY=your_clash_royale_api_key_here
CLASH_ROYALE_API_BASE_URL=https://api.clashroyale.com/v1

# API performance settings
API_TIMEOUT=10000
API_RETRIES=3
API_RETRY_DELAY=1000
API_RATE_LIMIT=100

# -----------------------------------------------------------------------------
# GOOGLE OAUTH CONFIGURATION
# -----------------------------------------------------------------------------
# Google OAuth 2.0 credentials for user authentication
# Setup instructions:
# 1. Go to Google Cloud Console: https://console.cloud.google.com/
# 2. Create a new project or select existing project
# 3. Enable Google Identity Services API
# 4. Configure OAuth consent screen (External user type)
# 5. Create OAuth 2.0 Client ID credentials (Web application)
# 6. Configure authorized JavaScript origins:
#    - http://localhost:3000 (development frontend)
#    - http://localhost:8000 (development backend)
#    - Add your production domain when ready
# 7. Configure authorized redirect URIs:
#    - http://localhost:3000/auth/callback (development)
#    - Add production callback URL when ready

GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
REACT_APP_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# -----------------------------------------------------------------------------
# SECURITY CONFIGURATION
# -----------------------------------------------------------------------------
# JWT authentication settings
# Generate a secure random string for production (minimum 32 characters)
JWT_SECRET_KEY=your_jwt_secret_key_minimum_32_characters_long
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=15
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Session configuration
SESSION_TIMEOUT=60

# -----------------------------------------------------------------------------
# PERFORMANCE & CACHING
# -----------------------------------------------------------------------------
# Cache settings (in seconds)
CACHE_TTL=3600

# Request timeouts (in milliseconds)
REQUEST_TIMEOUT=30000
DATABASE_TIMEOUT=5000

# -----------------------------------------------------------------------------
# DOCKER CONFIGURATION
# -----------------------------------------------------------------------------
# Docker Compose service names (used when ENVIRONMENT=docker)
DOCKER_DB_SERVICE=database
DOCKER_BACKEND_SERVICE=backend
DOCKER_FRONTEND_SERVICE=frontend

# Docker network settings
DOCKER_NETWORK=clash_deck_builder_network

# -----------------------------------------------------------------------------
# DEVELOPMENT TOOLS
# -----------------------------------------------------------------------------
# Development-specific settings
FAST_REFRESH=true
REACT_STRICT_MODE=true

# Testing configuration
TEST_DATABASE_URL=mysql://test_user:test_pass@localhost:3306/clash_deck_builder_test

# -----------------------------------------------------------------------------
# LOGGING & MONITORING
# -----------------------------------------------------------------------------
# Log configuration
LOG_FORMAT=json
LOG_FILE_PATH=./logs/app.log
LOG_MAX_SIZE=10MB
LOG_BACKUP_COUNT=5

# Monitoring
HEALTH_CHECK_INTERVAL=30
METRICS_ENABLED=false

# -----------------------------------------------------------------------------
# FEATURE FLAGS
# -----------------------------------------------------------------------------
# Enable/disable features
ENABLE_GOOGLE_AUTH=true
ENABLE_DECK_SHARING=false
ENABLE_ANALYTICS=false
ENABLE_RATE_LIMITING=true

# -----------------------------------------------------------------------------
# PRODUCTION OVERRIDES
# -----------------------------------------------------------------------------
# These will be overridden in production environment
# ENVIRONMENT=production
# DEBUG=false
# LOG_LEVEL=warning
# DB_HOST=production-db-host
# REACT_APP_API_BASE_URL=https://api.yourdomain.com
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com